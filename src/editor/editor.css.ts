import { style, flex } from '../styles';

export default {

  root: style(flex.content.stack),

};

/*  anux-editor {
  display: flex;
  flex: none;
  flex-direction: column;

  .anux-editor-field {
    padding: 0 0 2px;

    > div {
      display: flex;
      flex: auto;

      .MuiInputBase-root {
        font-size: inherit;
      }

      .MuiSelect-selectMenu {
        padding: 0;
        height: auto;
      }

      .MuiInputBase-input {
        height: 1.6em;
      }

      .anux-editor-field-progress {
        margin: -1px 0 0;
        padding: 1px 0 0;
        height: 1px;
      }
    }
  }
}*/
